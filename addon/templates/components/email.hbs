{{#unless @required}}
  <span class="cs-component-email--optional" data-test-cs-component-email-optional>
    Optional
  </span>
{{/unless}}
<input
  type="email"
  name="email"
  value={{value}}
  id="{{elementId}}-input"
  class="cs-component-email--input {{if @required "required"}}"
  required={{@required}}
  autocomplete="off"
  onchange={{action handleInput}}
>
<label
  for="{{elementId}}-input"
  class="cs-component-email--label {{if value "active"}}"
>
  <span>{{@label}}</span>
</label>

<div
  class="cs-component-email--validation {{if (not validationMessage) "hidden"}} {{if invalid "invalid"}}"
  data-test-cs-component-email-validation
>
  {{validationMessage}}
</div>